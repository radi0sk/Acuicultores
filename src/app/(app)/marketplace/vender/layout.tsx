
"use client";

// Este layout ahora está vacío porque la lógica se ha movido al layout principal de /marketplace
export default function VenderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

    